export const environment = {
  production: false,
  apiUrl: 'https://localhost:7252/api',
  apiVersion: 'v1',
  /** When true, login uses `src/mock-data/auth` instead of the API. Set false to use the real backend. */
  useMockAuth: true
};

